# ADR 0001：采用 modular monolith

状态：已接受

## 决策

第一版后端使用单一 Spring Boot 部署单元，并通过 Spring Modulith 维持业务模块边界。

## 原因

当前团队和业务范围不需要微服务的独立部署复杂度。模块化单体可以保留清晰边界，同时降低本地开发、事务、测试和运维成本。

## 影响

新增代码按业务能力进入顶层 package。跨模块依赖必须可见且无循环。只有在出现经过验证的独立扩缩容或部署需求后，才重新评估拆分。
